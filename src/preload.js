'use strict';

// Preload script — runs in the renderer context with Node access.
// With nodeIntegration: true, this is minimal. It's kept for potential
// future use (e.g., exposing safe APIs if nodeIntegration is disabled).

// Nothing needed here — renderer.js uses require('electron').ipcRenderer directly.
